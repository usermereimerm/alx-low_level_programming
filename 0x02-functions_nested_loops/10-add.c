#include "main.h"

/**
 * add - adds two integers
 * @a: the first integer
 * @b: the second integer
 * Return: the sum of the two integers
 */
int add(int a, int b);
{
	int a, b, sum;

	sum = a + b;
	return (sum);
}
