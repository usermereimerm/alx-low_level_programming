#include "main.h"

/**
 * _isalpha - tests for alphabetic character.
 * @c: character to be tested
 *
 * Return: 0 if successfuk
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z')
			return (1);
			else
			return (0);
}
