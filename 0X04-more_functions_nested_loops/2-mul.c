#include "main.h"

/**
 * mul - returns the product of to given integers
 * @a: the first integer
 * @b: the second integer to multiply a
 *
 * Return: the result of multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
