#include "main.h"

/**
 * strcpy - copies a string from one array to another
 * @dest: array to store copied string
 * @src: source to copy string from
 * Return: dest
 */
char _strcpy(char *dest, char *src)
{
	int index, len;

	while (src[index++)
		len++;
	for (index = 0; index < len; index++)
		dest[index] = src[index];
	return (dest);
}
